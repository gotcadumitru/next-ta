import Loader from '@/shared/ui/Loader'
import { FC } from 'react'

const HolidayLoading: FC = () => (
  <div className='container text-align-center'>
    <Loader />
  </div>
)
export default HolidayLoading
